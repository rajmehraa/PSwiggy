import { useState } from "react"

function Form(){
    const[name,setName]=useState('')
    const[age,setAge]=useState

    const handle=()=>{
        1 localStorage 
              contxt api
              state store
              localStorage .setItem
              localStorage.getItem
        2 next page 
              react router

    }
    const handlechgange()=>{
              
    }
    return(
        <Form>
        <input text="" key="hello"  onChange={()=>handlechgange(key,event) } >Text</input>
        <input text="" key="username"  onChange={(event)=>setName(event.target.value) } >Text</input>
        <input text="" key="hello3"  onChange={(event)=>setName(event.target.value) } >Text</input>
        

        <button onClick={}>next</button>

        </Form>
    )
}